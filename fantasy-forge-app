<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fantasy Forge AI - Create Bold Adult Stories</title>
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Load Google Fonts: Inter (Body) and Michroma (Headlines) -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Michroma&display=swap" rel="stylesheet">
    <style>
        /* Custom Tailwind Configuration for Neon Colors */
        :root {
            --red-accent: #FF0033;
            --purple-accent: #9900FF;
            --dark-gray: #0d0d0d;
        }

        /* Set global font and background */
        body {
            font-family: 'Inter', sans-serif;
            background-color: #000000;
            color: #f0f0f0;
            min-height: 100vh;
        }

        /* Custom Neon Glow Utilities */
        .text-red-neon { color: var(--red-accent); text-shadow: 0 0 10px var(--red-accent); }
        .text-purple-neon { color: var(--purple-accent); text-shadow: 0 0 10px var(--purple-accent); }

        /* General Container for Centering Content */
        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        /* --- Age Gate Overlay Styling --- */
        #age-gate-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.95);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 50;
        }

        .age-gate-box {
            background: var(--dark-gray);
            padding: 3rem 2rem;
            border: 2px solid var(--purple-accent);
            box-shadow: 0 0 30px var(--purple-accent); /* Neon Glow */
            text-align: center;
            border-radius: 12px;
            max-width: 90%;
            width: 400px;
        }

        /* --- CTA Button Base Styling --- */
        .cta-button {
            display: block;
            width: 100%;
            padding: 1rem 2rem;
            border-radius: 8px;
            font-weight: 700;
            text-transform: uppercase;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: none;
            cursor: pointer;
        }

        /* Primary CTA Button (Red Accent) */
        .primary-cta {
            background: var(--red-accent);
            color: white;
            box-shadow: 0 0 15px var(--red-accent);
        }

        .primary-cta:hover {
            background: #ff4d66;
            box-shadow: 0 0 30px #ff4d66;
            transform: translateY(-2px);
        }

        /* Secondary CTA Button (Purple Accent) */
        .secondary-cta {
            background: var(--dark-gray);
            color: var(--purple-accent);
            border: 2px solid var(--purple-accent);
            box-shadow: 0 0 10px var(--purple-accent) inset;
        }

        .secondary-cta:hover {
            background: var(--purple-accent);
            color: var(--dark-gray);
            box-shadow: 0 0 30px var(--purple-accent);
        }
        
        /* LLM Helper Button (Subtle Glow) */
        .llm-helper-button {
            background-color: #1a0033; /* Darker Purple */
            color: var(--purple-accent);
            border: 1px solid var(--purple-accent);
            padding: 0.75rem 1.5rem;
            border-radius: 6px;
            font-weight: 600;
            transition: all 0.2s;
            box-shadow: 0 0 5px var(--purple-accent) inset;
        }
        .llm-helper-button:hover {
            box-shadow: 0 0 15px var(--purple-accent);
            transform: scale(1.02);
        }

        /* TTS Playback Button (Special styling for audio feature) */
        #play-dialogue-btn {
            background-color: var(--red-accent);
            color: white;
            border: 1px solid var(--red-accent);
            box-shadow: 0 0 5px var(--red-accent);
            padding: 0.5rem 1rem;
            margin-top: 1rem;
            font-weight: 700;
            transition: all 0.2s;
            border-radius: 6px;
        }
        #play-dialogue-btn:hover:not(:disabled) {
            box-shadow: 0 0 15px var(--red-accent);
        }
        #play-dialogue-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }
        
        /* --- Hero Section & Banner Styling --- */
        .hero-section {
            min-height: 90vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 4rem 1rem;
            position: relative; /* Needed for positioning internal banners */
            overflow: hidden;
            border-bottom: 2px solid rgba(153, 0, 255, 0.1); /* Subtle border */
        }

        .hero-banner-visuals {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;
        }

        .hero-banner-visuals img {
            position: absolute;
            height: 100%;
            width: auto;
            object-fit: cover;
            opacity: 0.2; /* Darken and blend */
            filter: blur(2px) grayscale(50%); /* Cinematic effect */
            transition: opacity 0.5s ease;
        }
        
        /* Left Image Positioning (Mobile: smaller, Desktop: prominent left) */
        .hero-banner-visuals .img-left {
            top: 0;
            left: 0;
            transform: scale(0.6) translateX(-50%) rotate(5deg);
            transform-origin: top left;
        }
        
        /* Right Image Positioning (Mobile: smaller, Desktop: prominent right) */
        .hero-banner-visuals .img-right {
            bottom: 0;
            right: 0;
            transform: scale(0.6) translateX(50%) rotate(-5deg);
            transform-origin: bottom right;
        }

        @media (min-width: 1024px) {
            .hero-banner-visuals .img-left {
                height: 120%;
                opacity: 0.15;
                transform: scale(1) translateX(0) rotate(5deg);
                left: -20%;
                top: -10%;
            }
            .hero-banner-visuals .img-right {
                height: 120%;
                opacity: 0.15;
                transform: scale(1) translateX(0) rotate(-5deg);
                right: -20%;
                bottom: -10%;
            }
        }

        .hero-content {
            position: relative;
            z-index: 10; /* Ensure text is above the visuals */
            max-width: 4xl;
            margin: auto;
        }

        .llm-output-box {
            white-space: pre-wrap;
            text-align: left;
            padding: 1rem;
            background-color: #080808;
            border: 1px solid rgba(153, 0, 255, 0.2);
            border-radius: 8px;
            max-height: 400px;
            overflow-y: auto;
            margin-top: 1rem;
            font-size: 0.95rem;
        }

        .dialogue-speaker {
            font-weight: bold;
            color: var(--red-accent);
            margin-top: 0.5rem;
            display: block;
        }
    </style>
</head>
<body class="bg-black">

    <!-- Age Verification Overlay (Initially Visible) -->
    <div id="age-gate-overlay">
        <div class="age-gate-box">
            <h2 class="text-3xl font-bold mb-4 text-purple-neon" style="font-family: 'Michroma', sans-serif;">
                Access Restricted (18+)
            </h2>
            <p class="mb-6 opacity-80">
                You must be 18 years or older to access **Fantasy Forge AI**. 
                Content includes mature narratives and themes.
            </p>
            <button id="confirm-age-btn" class="cta-button primary-cta mb-3">
                I Confirm I Am 18+
            </button>
            <p class="text-xs opacity-50">
                By clicking, you confirm you meet the legal age requirement for this content.
            </p>
        </div>
    </div>

    <!-- Main Content (Hidden until age is confirmed) -->
    <div id="main-content" class="hidden">
        
        <!-- Header & Navigation -->
        <nav class="container flex justify-between items-center py-4 border-b border-gray-900 sticky top-0 bg-black/90 backdrop-blur-sm z-40">
            <a href="#hero" class="text-2xl font-bold text-red-neon" style="font-family: 'Michroma', sans-serif;">
                Fantasy Forge
            </a>
            <div class="flex space-x-4">
                <a href="#generator" class="hover:text-purple-neon transition duration-200 hidden sm:inline">Create</a>
                <a href="#library" class="hover:text-purple-neon transition duration-200 hidden sm:inline">Library</a>
                <a href="#premium" class="hover:text-red-neon transition duration-200 font-bold">Premium</a>
            </div>
        </nav>

        <!-- --- 1. Hero Landing Page --- -->
        <section id="hero" class="hero-section">
            
            <!-- Dynamic Background Banner Visuals: Bold Female Characters -->
            <div class="hero-banner-visuals">
                <!-- Placeholder for Bold Female Character 1 (Red Tones) -->
                <img class="img-left hidden lg:block" 
                     src="https://placehold.co/800x1200/220000/FF0033?text=BOLD+CHARACTER+A" 
                     alt="Cinematic Female Character Visual 1"
                     onerror="this.style.display='none'"> 

                <!-- Placeholder for Bold Female Character 2 (Purple Tones) -->
                <img class="img-right hidden lg:block" 
                     src="https://placehold.co/800x1200/08001a/9900FF?text=BOLD+CHARACTER+B" 
                     alt="Cinematic Female Character Visual 2"
                     onerror="this.style.display='none'">
            </div>

            <!-- Foreground Content -->
            <div class="hero-content max-w-4xl mx-auto">
                <h1 class="text-5xl sm:text-7xl lg:text-8xl font-extrabold mb-6 leading-tight" style="font-family: 'Michroma', sans-serif;">
                    Forge Your Fantasies.
                    <span class="text-red-neon block mt-2">
                        Beyond the Page.
                    </span>
                </h1>
                <p class="text-lg sm:text-xl opacity-80 mb-10 max-w-2xl mx-auto">
                    Create **Bold Adult Stories** with AI. Instantly generate cinematic video, background music, lip-synced dialogues, and immersive audio narration.
                </p>
                <a href="#generator" class="cta-button primary-cta sm:w-auto sm:inline-block">
                    Start Creating
                </a>
                <p class="mt-4 text-sm opacity-60">No credit card required to start generating short stories.</p>
            </div>
        </section>

        <!-- --- 2. Story Generator Page --- -->
        <section id="generator" class="py-20 container">
            <h2 class="text-4xl font-bold text-center mb-12 text-purple-neon" style="font-family: 'Michroma', sans-serif;">
                The Forge: Text to Cinematic Story
            </h2>
            <div class="bg-dark-gray p-6 sm:p-10 rounded-xl border border-purple-accent/50 shadow-xl">
                <label for="prompt-input" class="block text-lg font-medium mb-3">Your Core Idea (A detailed prompt produces the best videos)</label>
                <textarea id="prompt-input" rows="4" placeholder="Describe your scene, characters, and desired tone here..."
                    class="w-full p-4 mb-3 bg-black text-white border border-purple-accent/30 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-accent transition"
                >A rogue assassin discovers a hidden magical library in the cyberpunk city, seeking knowledge to reverse a curse placed on her. Her handler attempts to stop her.</textarea>

                <!-- LLM Helper Buttons -->
                <div class="flex flex-col sm:flex-row space-y-3 sm:space-y-0 sm:space-x-4 mb-6">
                    <button onclick="refinePromptWithGemini()" class="w-full llm-helper-button flex-1">
                        ✨ Narrative Refiner (Get cinematic ideas)
                    </button>
                    <button onclick="generateDialogueScript()" class="w-full llm-helper-button flex-1">
                        ✨ Dialogue Script Forge (Generate a tense conversation)
                    </button>
                </div>
                
                <!-- LLM Output Display -->
                <div id="llm-output-container" class="mb-6">
                    <!-- Output will be displayed here -->
                </div>


                <div class="flex flex-col sm:flex-row space-y-4 sm:space-y-0 sm:space-x-4">
                    <select id="quality-select" class="w-full sm:w-1/3 p-3 bg-black text-white border border-purple-accent/30 rounded-lg appearance-none cursor-pointer transition">
                        <option value="720p">Standard Quality (720p - Free)</option>
                        <option value="1080p">HD Quality (1080p - Premium)</option>
                        <option value="4K_PREMIUM">Cinematic 4K (Premium Max)</option>
                    </select>

                    <button onclick="handleGeneration()" class="w-full sm:w-2/3 cta-button primary-cta">
                        <svg class="w-5 h-5 inline-block mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
                        Forge Story & Video
                    </button>
                </div>
            </div>

            <!-- Conceptual Status Box -->
            <div id="status-message" class="mt-8 p-4 rounded-lg text-center bg-gray-900 border border-gray-700 hidden transition-all duration-500">
                Processing...
            </div>
        </section>

        <!-- --- 3. Feature Showcase --- -->
        <section class="py-20 container border-t border-gray-900">
            <div class="grid md:grid-cols-3 gap-10">
                <div class="p-6 bg-dark-gray rounded-xl border border-red-accent/50 shadow-lg hover:shadow-red-neon/30 transition duration-300">
                    <h3 class="text-2xl font-bold mb-3 text-red-neon">Cinematic Video</h3>
                    <p class="opacity-80">AI converts your narrative script into visually stunning, film-grade scenes with dynamic camera work and custom color grading.</p>
                </div>
                <div class="p-6 bg-dark-gray rounded-xl border border-purple-accent/50 shadow-lg hover:shadow-purple-neon/30 transition duration-300">
                    <h3 class="text-2xl font-bold mb-3 text-purple-neon">Lip-Sync Dialogue</h3>
                    <p class="opacity-80">Characters speak their lines naturally, featuring perfectly synced facial animations for the highest level of realism.</p>
                </div>
                <div class="p-6 bg-dark-gray rounded-xl border border-red-accent/50 shadow-lg hover:shadow-red-neon/30 transition duration-300">
                    <h3 class="text-2xl font-bold mb-3 text-red-neon">Consistent Characters</h3>
                    <p class="opacity-80">Lock your favorite character designs so they maintain the same appearance across new scenes, new stories, and new videos.</p>
                </div>
            </div>
        </section>

        <!-- --- 4. Library & Premium CTA --- -->
        <section id="premium" class="py-20 bg-gray-900/50">
            <div class="container text-center">
                <h2 class="text-4xl font-bold mb-4 text-purple-neon" style="font-family: 'Michroma', sans-serif;">
                    Unlock the Full Power of the Forge
                </h2>
                <p class="text-lg opacity-80 mb-10 max-w-3xl mx-auto">
                    Go Premium for longer stories, cinematic 4K video quality, and dedicated character design slots.
                </p>
                <a href="#" class="cta-button secondary-cta sm:w-auto sm:inline-block">
                    View Premium Tiers
                </a>
            </div>
        </section>


        <!-- Footer -->
        <footer class="container py-8 text-center text-sm opacity-60 border-t border-gray-900 mt-10">
            <p>&copy; 2025 Fantasy Forge AI. All rights reserved. | <a href="#" class="hover:text-red-accent">Terms</a> | <a href="#" class="hover:text-red-accent">Privacy</a></p>
            <p class="mt-2">Join the discussion on our <a href="#" class="hover:text-purple-accent font-bold">Community Forum</a></p>
        </footer>

    </div>

    <script>
        const ageGateOverlay = document.getElementById('age-gate-overlay');
        const confirmAgeBtn = document.getElementById('confirm-age-btn');
        const mainContent = document.getElementById('main-content');
        const statusMessage = document.getElementById('status-message');
        const promptInput = document.getElementById('prompt-input');
        const llmOutputContainer = document.getElementById('llm-output-container');
        
        // Gemini API Configuration
        // NOTE: You must replace the empty apiKey with your actual key when you publish this.
        const apiKey = ""; 
        const apiUrlBase = "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-05-20:generateContent";

        // Global state for dialogue, used by the playback feature
        let lastGeneratedDialogue = [];

        // Define voices for the demo characters (Agent and Handler)
        const VOICE_MAP = {
            'Agent': 'Puck',    // Upbeat, quick voice
            'Handler': 'Kore'   // Firm, mature voice
        };

        // --- Audio Utility Functions (for TTS PCM to WAV conversion) ---

        /** Converts a base64 encoded string to an ArrayBuffer. */
        function base64ToArrayBuffer(base64) {
            const binaryString = atob(base64);
            const len = binaryString.length;
            const bytes = new Uint8Array(len);
            for (let i = 0; i < len; i++) {
                bytes[i] = binaryString.charCodeAt(i);
            }
            return bytes.buffer;
        }

        /** Writes a string to a DataView at a specific offset. */
        function writeString(view, offset, str) {
            for (let i = 0; i < str.length; i++) {
                view.setUint8(offset + i, str.charCodeAt(i));
            }
        }

        /** Writes a 32-bit integer to a DataView. */
        function writeInt(view, offset, value) {
            view.setUint32(offset, value, true);
        }

        /** Writes a 16-bit integer to a DataView. */
        function writeShort(view, offset, value) {
            view.setUint16(offset, value, true);
        }

        /** Converts raw PCM 16-bit audio data to a WAV Blob. */
        function pcmToWav(pcm16, sampleRate) {
            const numChannels = 1; 
            const bitsPerSample = 16;
            const byteRate = sampleRate * numChannels * (bitsPerSample / 8);
            const blockAlign = numChannels * (bitsPerSample / 8);
            
            // 44 bytes for the header + 2 * number of samples (since it's 16-bit)
            const buffer = new ArrayBuffer(44 + pcm16.length * 2);
            const view = new DataView(buffer);

            // RIFF chunk
            writeString(view, 0, 'RIFF');
            writeInt(view, 4, 36 + pcm16.length * 2); // File size
            writeString(view, 8, 'WAVE');

            // fmt chunk
            writeString(view, 12, 'fmt ');
            writeInt(view, 16, 16);             // fmt sub-chunk size
            writeShort(view, 20, 1);            // Audio format (1 for PCM)
         
